import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Health Vector - AI Medical Encyclopedia',
  description: 'AI-powered medical encyclopedia chatbot using RAG technology for informational health content',
  keywords: 'health, medical, AI, chatbot, encyclopedia, symptoms, treatment',
  authors: [{ name: 'Bhavya Shah' }],
}

export const viewport = {
  width: 'device-width',
  initialScale: 1,
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
      </body>
    </html>
  )
}
